extends layout

block content
  .container
    h1 Edit Snowboard

    // Form to edit snowboard
    form(action=`/admin/snowboards/${snowboard.id}/edit` method="POST")
      .mb-3
        label(for="name") Name
        input#name.form-control(type="text" name="name" required value=snowboard.name)

      .mb-3
        label(for="model") Model
        input#model.form-control(type="text" name="model" required value=snowboard.model)

      .mb-3
        label(for="length") Length (cm)
        input#length.form-control(type="number" name="length" required min="0" value=snowboard.length)

      .mb-3
        label(for="material") Material
        input#material.form-control(type="text" name="material" value=snowboard.material)

      .mb-3
        label(for="price") Price ($)
        input#price.form-control(type="number" name="price" required step="0.01" min="0" value=snowboard.price)

      .mb-3
        label(for="imageUrl") Image URL
        input#imageUrl.form-control(type="url" name="imageUrl" value=snowboard.imageUrl)

      // Submit Button
      button.btn.btn-primary(type="submit") Save Changes

      // Cancel Button
      a.btn.btn-secondary(href="/admin/snowboards") Cancel
